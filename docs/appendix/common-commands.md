---
title: Common Commands
description: Review the shell commands most frequently used while working with BAOBAB locally and in automation workflows.
---

# Common Commands

The following commands are helpful when working with the repository and its container tooling:

```bash
# Start the documentation build locally
mkdocs serve

# Build the site for production output
mkdocs build --strict

# Create a Python virtual environment for local tooling
python -m venv .venv
```

